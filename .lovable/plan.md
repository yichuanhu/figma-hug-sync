

## 计划

修改任务/任务模板创建编辑表单中"执行目标"为单个机器人时的选择器交互。

### 现状定位
需要先定位：
1. 任务/任务模板表单中的机器人选择器组件（应为 `BotTargetSelector` 或类似）
2. 当前实现：可能是按组分层的 TreeSelect/Cascader
3. 期望：扁平 Select 列表，每项显示 `[组名] 机器人名`，"未分组" 处理无组归属的机器人

### 改动
- 将单个机器人选择器改为扁平 `Select`（带搜索）
- 选项 label 拼接 `[组名] + 机器人名`，无组归属显示 `[未分组]`
- 保留搜索能力（同时匹配组名与机器人名）
- 机器人组选择器不变

### 验收
1. 新建任务 → 执行目标=单个机器人 → 选择器为扁平列表
2. 每项格式 `[组名] 机器人名`，无分组显示 `[未分组]`
3. 搜索同时支持组名/机器人名
4. 切换为"机器人组"时仍按组列表显示

